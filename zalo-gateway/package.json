{
  "name": "gemini-zalo-gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "login:qr": "npm run build && node dist/qr-login.js"
  },
  "dependencies": {
    "zca-js": "2.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
