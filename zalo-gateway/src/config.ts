export type GatewayConfig={cookie?:unknown;imei?:string;userAgent:string;controllerId:string;accountId:string;bridgeBaseUrl:string;bridgeSecret:string;groupRefreshMs:number;outboxPollMs:number;controlPort:number};
function required(n:string){const v=process.env[n]?.trim();if(!v)throw new Error(`Missing required environment variable: ${n}`);return v;}
function number(n:string,f:number,min=1000){const v=Number(process.env[n]||f);return Number.isFinite(v)?Math.max(min,v):f;}
export function loadConfig():GatewayConfig{let cookie:unknown;const raw=process.env.ZALO_COOKIE_JSON?.trim();if(raw)try{cookie=JSON.parse(raw)}catch{throw new Error("ZALO_COOKIE_JSON must be valid JSON")}const base=process.env.ZALO_BRIDGE_BASE_URL?.trim()||`http://127.0.0.1:${process.env.PORT||"8000"}/internal/zalo`;return{cookie,imei:process.env.ZALO_IMEI?.trim(),userAgent:process.env.ZALO_USER_AGENT?.trim()||"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/145.0.0.0 Safari/537.36",controllerId:process.env.ZALO_CONTROLLER_ID?.trim()||"",accountId:process.env.ZALO_BOT_ACCOUNT_ID?.trim()||"zalo-bot",bridgeBaseUrl:base.replace(/\/$/,""),bridgeSecret:required("ZALO_BRIDGE_SECRET"),groupRefreshMs:number("ZALO_GROUP_REFRESH_MS",60000),outboxPollMs:number("ZALO_OUTBOX_POLL_MS",15000),controlPort:number("ZALO_CONTROL_PORT",9901,1024)};}
