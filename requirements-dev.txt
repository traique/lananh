-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
ruff>=0.9.0
# Chỉ dùng cho job train model offline (scripts/train_trend_model.py) - runtime
# Render KHÔNG cần sklearn: trend_model tự vô hiệu khi thiếu thư viện/model file.
scikit-learn>=1.4
